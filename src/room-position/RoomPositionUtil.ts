class RoomPositionUtil {
  // static isWall(roomPosition: RoomPosition): boolean {
  //   const terrain: Terrain[] = roomPosition.lookFor(LOOK_TERRAIN);
  //   if (terrain.length === 0) {
  //     return false;
  //   }
  //   return terrain.findIndex(element => element === 'wall') > -1;
  // }
}
